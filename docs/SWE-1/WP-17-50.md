# WP.17-50 — 검증 기준

> ASPICE v3.1 작업 산출물 양식 (Work Product Template)

---

## 문서 정보

| 항목         | 내용              |
| ------------ | ----------------- |
| **WP ID**    | WP.17-50          |
| **산출물명** | 검증 기준         |
| **버전**     | v1.0 / YYYY-MM-DD |
| **상태**     | Draft             |
| **작성자**   |                   |
| **승인자**   |                   |

---

## 관련 BP

| BP        | 프로세스                 | 역할                        |
| --------- | ------------------------ | --------------------------- |
| SWE.1.BP2 | 소프트웨어 요구사항 분석 | SW 요구사항 분석            |
| SWE.1.BP3 | 소프트웨어 요구사항 분석 | SW 요구사항 구조화/우선순위 |
| SWE.1.BP5 | 소프트웨어 요구사항 분석 | SW 요구사항 검증            |

---

## 내용

### 1. 검증 기준 목록

| 기준 ID  | 관련 REQ ID   | 검증 방법 (시험/분석/검토/시연)                                                                 | 합격 기준 / 측정 방법                                                                 | 상태  |
| -------- | ------------- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | ----- |
| VC-0002  | SWE-REQ-0002  | 단위 테스트: 다양한 부동소수점 케이스(정수, 소수, 경계값) 비교; 자동화된 테스트 스크립트(pytest) 사용      | 비교는 상대오차(relative error) ≤ 1e-9(또는 도메인별 별도 기준). 금융 요구 시 `decimal` 사용. | Draft |
| VC-0003  | SWE-REQ-0003  | 단위+통합: 계산 함수 호출(1/0) → `OperationResult` 반환 확인; REPL 통합 테스트(subprocess/pexpect)로 세션 유지 확인 | 함수 호출 시 `success=false`, `errorMsg`에 "divide" 포함; REPL은 예외로 종료하지 않음                   | Draft |
| VC-0011  | SWE-REQ-0011  | 성능 벤치마크: N=1000 반복, P50/P95 측정(pytest-benchmark 또는 timeit)                                                   | 테스트 환경(예: GitHub Actions ubuntu-latest, 2 vCPU, Python 3.10)에서 P50 < 100ms(문서화된 HW 필수)      | Draft |
| VC-0012  | SWE-REQ-0012  | 자원 프로파일: `psutil` 또는 `tracemalloc` 사용해 RSS/heap 측정 및 리포트; Python 환경 한계 확인 POC 필요           | Python(CPython)에서 64KB 목표는 "목표"로 취급: 미달성 시 POC 리포트 제출. 64KB 의무 시 언어 변경 필요      | Draft |
| VC-0001  | SWE-REQ-0001  | 단위 테스트: 정수/소수 연산 기본 벡터 케이스                                                                 | 모든 기본 연산 케이스 통과 및 예상값 일치                                                  | Draft |

### 2. 검증 방법 정의

#### 2.1 시험 (Test)

[시험을 통한 검증 방법을 기술할 것]

#### 2.2 분석 (Analysis)

[분석을 통한 검증 방법을 기술할 것]

#### 2.3 검토 (Review)

[검토를 통한 검증 방법을 기술할 것]

#### 2.4 시연 (Demonstration)

[시연을 통한 검증 방법을 기술할 것]

### 3. 시험 환경 요구사항

[검증 수행에 필요한 시험 환경(HW, SW, 도구 등)을 기술할 것]

---

## 변경 이력

| 버전 | 날짜       | 작성자 | 변경 내용 |
| ---- | ---------- | ------ | --------- |
| v1.0 | YYYY-MM-DD |        | 최초 작성 |
